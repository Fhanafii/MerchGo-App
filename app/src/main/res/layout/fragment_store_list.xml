<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <!-- Card Hijau Bagian Atas -->
        <com.google.android.material.card.MaterialCardView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:cardBackgroundColor="@color/primary"
            app:cardElevation="4dp"
            app:strokeWidth="0dp"
            app:shapeAppearanceOverlay="@style/ShapeAppearance.TopHeaderCard">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:paddingTop="@dimen/space_4"
                android:paddingBottom="@dimen/space_16">

                <!-- Toolbar (Back Button & Judul "Store List") -->
                <com.google.android.material.appbar.MaterialToolbar
                    android:id="@+id/toolbar"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    app:title="@string/store_list_title"
                    app:titleTextColor="@android:color/white"
                    app:navigationIconTint="@android:color/white"
                    android:minHeight="?attr/actionBarSize" />

                <!-- SearchView ("Cari Toko...") -->
                <androidx.appcompat.widget.SearchView
                    android:id="@+id/searchView"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="@dimen/space_16"
                    android:background="@drawable/bg_search_white"
                    app:iconifiedByDefault="false"
                    app:searchIcon="@drawable/ic_search"
                    android:queryHint="@string/search_store" />
            </LinearLayout>
        </com.google.android.material.card.MaterialCardView>

        <!-- Daftar Toko (Store List) -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerStores"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:layout_marginTop="@dimen/space_8"
            android:paddingStart="@dimen/space_16"
            android:paddingEnd="@dimen/space_16"
            android:clipToPadding="false"
            android:paddingBottom="@dimen/space_88" />
    </LinearLayout>

    <!-- Floating Action Button di Kanan Bawah -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabRefresh"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="@dimen/space_16"
        android:contentDescription="@string/add_store"
        android:src="@drawable/ic_storefab"
        app:backgroundTint="@color/primary"
        app:tint="@null" />
</androidx.coordinatorlayout.widget.CoordinatorLayout>
